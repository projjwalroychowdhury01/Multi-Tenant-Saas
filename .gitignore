# ── Python ────────────────────────────────────────────────────────────────────

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

.github
# commit graph outputs, ignore the extraction cache
graphify-out/
generate_report.py


# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# PEP 582
__pypackages__/

# SageMath parsed files
*.sage.py

# Cython debug symbols
cython_debug/

# ── Django ────────────────────────────────────────────────────────────────────

# SQLite — used locally; never commit DB files
db.sqlite3
db.sqlite3-journal

# Django local settings override
local_settings.py

# Collected static & media files — generated at deploy time, not source
staticfiles/
mediafiles/
media/

# ── Environment & Secrets — NEVER COMMIT ─────────────────────────────────────
.env
.env.*
!.env.example       # Keep the example template in version control
.envrc
secrets/
credentials/

# Certificate / key files
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
serviceAccountKey.json
*_secret.json

# ── Python Virtual Environments ───────────────────────────────────────────────
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# ── Dependency managers ───────────────────────────────────────────────────────

# Poetry — lock file should be committed for apps (reproducible installs)
# Remove the comment below if you want to exclude it:
# poetry.lock

# PDM
.pdm-python
.pdm-build/

# Pixi
.pixi

# UV
# uv.lock  # recommended to commit

# ── Celery ────────────────────────────────────────────────────────────────────
celerybeat-schedule
celerybeat-schedule.db
celerybeat.pid

# ── Redis ─────────────────────────────────────────────────────────────────────
dump.rdb
*.rdb

# ── Database dumps & backups ─────────────────────────────────────────────────
*.sql
*.dump
*.bak
*.backup
*.pgdump

# ── Docker ────────────────────────────────────────────────────────────────────
.docker/
docker-data/
# Leave docker-compose.yml and Dockerfile tracked in version control

# ── Logs ──────────────────────────────────────────────────────────────────────
logs/
*.log
*.log.*

# ── Type checkers & linters ───────────────────────────────────────────────────

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre
.pyre/

# pytype
.pytype/

# Ruff
.ruff_cache/

# ── Editors & IDEs ────────────────────────────────────────────────────────────

# VS Code — track shared settings, ignore machine-local state
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json

# JetBrains (PyCharm, IntelliJ, etc.)
.idea/
*.iml
*.iws
*.ipr
out/

# Vim / Neovim
*.swp
*.swo
*~
.netrwhist
Session.vim
.nvimrc.local

# Emacs
*#
.#*

# Sublime Text
*.sublime-project
*.sublime-workspace

# Cursor
.cursorignore
.cursorindexingignore

# Abstra
.abstra/

# Spyder
.spyderproject
.spyproject

# Rope
.ropeproject

# ── OS generated files ────────────────────────────────────────────────────────

# macOS
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# Linux
*~

# ── Temporary files ───────────────────────────────────────────────────────────
*.tmp
*.temp
tmp/
temp/

# ── Documentation builds ─────────────────────────────────────────────────────
docs/_build/
/site

# ── Jupyter / IPython ────────────────────────────────────────────────────────
.ipynb_checkpoints
profile_default/
ipython_config.py

# ── Miscellaneous tools ───────────────────────────────────────────────────────

# PyPI config
.pypirc

# PyBuilder
.pybuilder/
target/

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# ── Node / Frontend (future phases) ───────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.next/
.nuxt/

# ── Terraform / Infrastructure as Code (future phases) ────────────────────────
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
!*.tfvars.example
